import styled from "styled-components";

export const Container = styled.div`
  max-width: 960px;
  margin-right: auto;
  margin-left: auto;
  padding: 0px 15px;
  /* min-height: 100vh tells the app to make the container take the entire height of the screen */
  min-height: 100vh;
`;
